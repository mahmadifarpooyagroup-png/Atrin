namespace Atrin.Domain.Enums;

public enum TokenType
{
    AccessToken,
    RefreshToken
}

public enum UserStatus
{
    Active,
    Inactive,
    Suspended,
    Deleted
}

public enum Gender
{
    Male,
    Female,
    Other
}
